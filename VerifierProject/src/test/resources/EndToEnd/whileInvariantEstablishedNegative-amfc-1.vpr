method test() {
    var x: Int := 10

    while(x>0)
        invariant x < 0
    {
        x := x - 1
    }
}

domain X {
    function a(): Bool
}

method testWithFunction() {
    var x: Int := 10

    while(x>0)
        invariant a()
    {
        x := x - 1
    }
}
