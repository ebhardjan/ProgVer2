method test() {
    var b: Bool
    var x: Int

    x := 4
    if (x < 10) {
        b := x == 0
        x := 0
        if (b) {
            assert x == 0
            x:= 1
        }
    } else {
        assert false
    }
    assert x == 0
}
